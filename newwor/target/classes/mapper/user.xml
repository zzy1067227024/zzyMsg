<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.UserMssagemapper">

    <select id="selectAllUserFy" resultType="com.Mssage.UserMssage">
        select * from usermssage
        <where>
            1=1
            <if test="user_name != null and user_name !=''">
                and user_name = #{user_name}
            </if>
            <if test="user_no != null and user_no !=''">
                and user_no = #{user_no}
            </if>
            <if test="dept_no != null and dept_no !=''">
                and dept_no = #{dept_no}
            </if>
            <if test="dept_name != null and dept_name != ''">
                and dept_name = #{dept_name}
            </if>
            <if test="user_rolename != null and user_rolename !=''">
                and user_rolename = #{user_rolename}
            </if>
            <if test="user_roleno != null and user_roleno != ''">
                and user_roleno = #{user_roleno}
            </if>
        </where>
        limit #{page},#{size}
    </select>


    <select id="userAllcount" resultType="long">
        select count(1) from usermssage
        <where>
            1=1
            <if test="user_name != null and user_name !=''">
                and user_name = #{user_name}
            </if>
            <if test="user_no != null and user_no !=''">
                and user_no = #{user_no}
            </if>
            <if test="dept_no != null and dept_no !=''">
                and dept_no = #{dept_no}
            </if>
            <if test="dept_name != null and dept_name != ''">
                and dept_name = #{dept_name}
            </if>
            <if test="user_rolename != null and user_rolename !=''">
                and user_rolename = #{user_rolename}
            </if>
            <if test="user_roleno != null and user_roleno != ''">
                and user_roleno = #{user_roleno}
            </if>
        </where>
    </select>
    <select id="selectAllUserMssage" resultType="Map">
        select * from usermssage
        <where>
            1=1
            <if test="user_name != null and user_name !=''">
                and user_name = #{user_name}
            </if>
            <if test="user_no != null and user_no !=''">
                and user_no = #{user_no}
            </if>
            <if test="dept_no != null and dept_no !=''">
                and dept_no = #{dept_no}
            </if>
            <if test="dept_name != null and dept_name != ''">
                and dept_name = #{dept_name}
            </if>
            <if test="user_rolename != null and user_rolename !=''">
                and user_rolename = #{user_rolename}
            </if>
            <if test="user_roleno != null and user_roleno != ''">
                and user_roleno = #{user_roleno}
            </if>
        </where>
    </select>

    <insert id="insertUserMssage">
    insert into usermssage(
    <if test="user_name != null and user_name !=''">
        user_name,
    </if>
    <if test="user_pw != null and user_pw !=''">
        user_pw,
    </if>
    <if test="dept_no != null and dept_no !=''">
        dept_no,
    </if>
    <if test="dept_name != null and dept_name !=''">
        dept_name,
    </if>
    <if test="user_rolename != null and user_rolename !=''">
        user_rolename,
    </if>
    <if test="user_roleno != null and user_roleno !=''">
        user_roleno,
    </if>
    user_no
    )
    values (
        <if test="user_name != null and user_name !=''">
            #{user_name},
        </if>
        <if test="user_pw != null and user_pw !=''">
            #{user_pw},
        </if>
        <if test="dept_no != null and dept_no !=''">
            #{dept_no},
        </if>
        <if test="dept_name != null and dept_name !=''">
            #{dept_name},
        </if>
        <if test="user_rolename != null and user_rolename !=''">
            #{user_rolename},
        </if>
        <if test="user_roleno != null and user_roleno !=''">
            #{user_roleno},
        </if>
    #{user_no}
        )
    </insert>
    <delete id="deleteUserMssage" parameterType="Map">
		DELETE FROM usermssage WHERE id = #{id}
	</delete>
    <update id="updateUserMssage" parameterType="Map">
    UPDATE usermssage
    <set>
        user_no = #{user_no}
    <if test="user_name != null and user_name !=''">
        ,user_name = #{user_name}
    </if>
    <if test="user_pw != null and user_pw !=''">
        ,user_pw = #{user_pw}
    </if>
    <if test="dept_no != null and dept_no !=''">
         ,dept_no = #{dept_no}
    </if>
    <if test="dept_name != null and dept_name !=''">
        ,dept_name = #{dept_name}
    </if>
    <if test="user_rolename != null and user_rolename !=''">
        ,user_rolename = #{user_rolename}
    </if>
    <if test="user_roleno != null and user_roleno !=''">
        ,user_roleno = #{user_roleno}
    </if>
    </set>
        <where>
           id = #{id}
        </where>
    </update>
</mapper>
